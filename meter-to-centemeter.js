function meterToCentemeter(meter) {
    const centemeter = meter * 100;
    return centemeter;
}

const centemeterCalculate = meterToCentemeter(20);
// console.log(centemeterCalculate);

// kg to gm
function kgToGm(kg) {
    const gram = kg * 1000;
    return gram;
}
const result = kgToGm(20)

const kg = result / 1000;
console.log(kg);
